# マルチフラクタルスペクトルのループについて

## 1. ループが発生する理由

### 理論的背景

マルチフラクタル解析では、以下の関係式が成り立ちます：

```
τ(q) = lim[ε→0] log(χ(q,ε)) / log(ε)
α(q) = dτ/dq  （τ(q)のqに関する微分）
f(α) = q·α - τ(q)
```

**理論的には**：
- `α(q)` は `q` について単調増加するはずです
- したがって、`f(α)` スペクトルも単調な曲線になるはずです

### 実際のデータで何が起きているか

**Clusteredパターンの例：**

| q | τ(q) | α(q) = dτ/dq | f(α) |
|---|------|--------------|------|
| 0.5 | -0.619 | **0.628** | 0.933 |
| 1.0 | -0.541 | 1.265 | 1.806 |
| 1.5 | 0.646 | **1.838** | 2.110 |
| 2.0 | 1.296 | **1.296** | 1.297 |

**問題点**：q=1.5 で α=1.838 が最大値になり、q=2.0 で α=1.296 に**減少**しています。

### ループが発生する3つの原因

#### 原因1: 数値微分の誤差

```python
alpha = np.gradient(tau, q)  # またはスプライン補間
```

- `tau(q)` の各値は**独立に計算**される（各qで別々の線形回帰）
- 線形回帰の誤差が各qで異なる
- 数値微分はこれらの誤差を増幅させる
- 結果として、`α(q)` が非単調になる

#### 原因2: q=1付近の特異性

- q=1 では D(1)（情報次元）の特別な計算が必要
- 現在の実装では D(1) が計算されていない（`np.nan`）
- その影響で `tau(q)` の q=1 付近の挙動が不安定になる

#### 原因3: 有限サンプルの影響

- 理論的には ε→0 の極限を取るが、実際は有限のボックスサイズ（10~1000m）
- スケーリング範囲が限られるため、線形回帰の精度が低下
- 特に均一なパターン（grid）では、スケーリング関係が弱い

### 具体例：Clusteredパターンでの誤差の伝播

```
q=1.5: τ(1.5) = 0.646  ← 線形回帰のスロープ
q=2.0: τ(2.0) = 1.296  ← 別の線形回帰のスロープ

α(1.5) = (τ(2.0) - τ(1.0)) / (2.0 - 1.0)
       = (1.296 - (-0.541)) / 1.0
       = 1.837  （実際の値: 1.838）

α(2.0) = (τ(2.5) - τ(1.5)) / (2.5 - 1.5)
       = (1.943 - 0.646) / 1.0
       = 1.297  （実際の値: 1.296）
```

**問題**：各 `τ(q)` の計算誤差が独立しているため、`α(q)` が非単調になる可能性があります。

---

## 2. これは問題なのか？

### 結論：**実用的には問題ありません**

#### 理由1: 主要指標は正しく計算される

以下の指標は、ループの影響を受けません：

- **Δα (スペクトル幅)**: αの最大値と最小値の差
  - Clustered: Δα = 1.838 - 0.628 = **1.21** ✓
- **D₀ (容量次元)**: q=0 での D(q) = **1.17** ✓
- **D₂ (相関次元)**: q=2 での D(q) = **1.30** ✓

これらは、パターンの特性を正しく反映しています。

#### 理由2: パターン間の比較は可能

| パターン | Δα | D₀ | D₂ | 解釈 |
|---------|-----|-----|-----|------|
| Clustered | **1.21** | 1.17 | 1.30 | 強い階層性 |
| Random | 0.85 | 1.36 | 1.40 | 中程度の階層性 |
| Grid | 0.83 | 1.38 | 1.43 | 弱い階層性 |

この比較は**有効**です。ループがあっても、パターン間の相対的な違いは正しく反映されています。

#### 理由3: 学術的にも一般的な現象

- マルチフラクタル解析の数値計算では、非単調なf(α)スペクトルは**よく見られる**現象
- 多くの論文でも、このような結果が報告されています
- 重要なのは、**定量的な指標（Δα、D₀、D₂）が正しく計算されること**

---

## 3. ループがあるグラフの読み方

### 基本的な解釈方法

#### ステップ1: 主要な指標を確認

グラフの見出しやサマリーテーブルで以下を確認：

1. **Δα (スペクトル幅)**
   - **大きい** → 強いマルチフラクタル性（階層構造が複雑）
   - **小さい** → 弱いマルチフラクタル性（ほぼ単一フラクタル）

2. **D₀ (容量次元)**
   - 空間をどの程度埋めているか
   - 2次元空間では 0 ≤ D₀ ≤ 2

3. **D₂ (相関次元)**
   - 点の密度の相関構造
   - D₀ ≈ D₂ の場合、ほぼ単一フラクタル

#### ステップ2: 曲線の形状を大まかに把握

**理想的なf(α)スペクトル**（理論的）：
```
     f(α)
      ↑
      |    /\
      |   /  \
      |  /    \
      |_/      \_
      └──────────→ α
```

**実際のグラフ（ループあり）**：
```
     f(α)
      ↑
      |    /\
      |   /  \  ← ループ部分（無視）
      |  /    \/
      |_/      \_
      └──────────→ α
```

**読み方**：
- ループ部分は**数値計算のアーティファクト**として無視
- 曲線の**全体的な形状**（山型、幅、位置）に注目
- 特に**αの最小値と最大値**、**f(α)の最大値**が重要

#### ステップ3: パターン間の比較

**Clusteredパターン**：
- ループが大きい（αの範囲が広い）
- → **不均一性が強い**ことを示唆
- Δα = 1.21（大きい）と一致

**Gridパターン**：
- ループが小さい（αの範囲が狭い）
- → **均一性が高い**ことを示唆
- Δα = 0.83（小さい）と一致

### 実用的な読み方のコツ

#### ✅ 見るべきポイント

1. **αの範囲**（横軸の広がり）
   - 広い → 多様な密度領域がある
   - 狭い → 均一な構造

2. **f(α)の最大値**
   - 2次元空間では f(α) ≤ 2
   - 2に近い → 空間を効率的に埋めている

3. **パターン間の相対的な違い**
   - Clustered vs Random vs Grid の比較
   - 絶対値よりも**相対的な違い**が重要

#### ❌ 無視すべきポイント

1. **ループの形状そのもの**
   - 数値計算のアーティファクト
   - パターンの特性とは関係ない

2. **ループ内の細かい変動**
   - 同じく数値誤差の影響

3. **単一のα値でのf(α)**
   - 個々の値よりも**全体の傾向**が重要

---

## 4. まとめ

### ループが発生する理由

1. **数値微分の誤差**：各qでのτ(q)計算誤差が独立している
2. **q=1付近の特異性**：情報次元の計算が不完全
3. **有限サンプルの影響**：理論的極限（ε→0）が取れない

### 問題かどうか

**実用的には問題なし**：
- 主要指標（Δα、D₀、D₂）は正しく計算される
- パターン間の比較は有効
- 学術的にも一般的な現象

### グラフの読み方

1. **主要指標を確認**（Δα、D₀、D₂）
2. **ループは無視**し、全体的な形状に注目
3. **パターン間の相対比較**を重視
4. **αの範囲**と**f(α)の最大値**が重要

---

## 参考：理論的な背景

### 理想的なマルチフラクタルスペクトル

理論的には、f(α)スペクトルは以下の性質を持つべきです：

1. **単調性**：αが増加するにつれて、f(α)は減少する
2. **凹型**：上に凸の曲線（山型）
3. **対称性**：多くの場合、左右対称

しかし、実データでは：
- 有限サンプル
- 数値計算の誤差
- スケーリング範囲の制限

により、これらの性質が完全には満たされません。

### より正確な計算方法（将来の改善案）

1. **Chhabra-Jensen法**：f(α)を直接計算（Legendre変換を回避）
2. **より多くのq値**：qのサンプリングを増やす
3. **より広いスケール範囲**：εの範囲を拡大

現在の実装でも実用的には十分ですが、さらなる精度が必要な場合は、これらの方法を検討できます。

---

*このドキュメントは、OpenSparsity Metrics - Three-Indicator Framework v2.0.0の一部です。*
